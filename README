================================================================
 A PHP client library for ROMA
 Copyright (C) 2010 Rakuten, Inc. 
================================================================

This library is written in almost C++, provides an interface to a WEB applications.

== Features

* suitable access to a ROMA nodes by routing information
* has a connection management
* following a failover in server

== Supported commands

* memcached commands
  * get, set, delete
* list commands (plugin commands of ROMA)
  * alist_sized_insert, alist_join, alist_delete, alist_delete_at
